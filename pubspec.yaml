name: vpn_info
description: "A Flutter package which provides connected vpn information."
version: 0.0.3
homepage: https://github.com/AdvaitKale01/vpn_info

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
